import React from 'react';

const UserContext = React.createContext();
//a way to share data globally across components without having to pass props manually at every level.

export default UserContext;